#include<iostream>

using namespace std;

int main()
{
    int n,len;

    cout << "Enter the number of rows: ";
    cin >> n;

    switch(n)
    {
        case 1: len=1; break;

        case 2: len=3; break;

        case 3: len=5; break;

        case 4: len=8; break;

        case 5: len=14; break;

        default: {
            if(n%2==0) len=
        }

    }

    return 0;
}
